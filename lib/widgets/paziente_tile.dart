import 'package:flutter/material.dart';

class PazienteTile extends StatelessWidget {
  const PazienteTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
      color: Colors.black38,
    );
  }
}
